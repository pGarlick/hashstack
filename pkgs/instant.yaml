extends: [pip_package]
dependencies:
  build: [numpy, six, subprocess32]
  run: [cmake, flufl_lock, numpy, swig, six, subprocess32]

sources:
- key: tar.gz:35piziygkrx5d3q2fdrwwyof2rsfnxel
  url: https://bitbucket.org/fenics-project/instant/downloads/instant-2016.2.0.tar.gz
